# 测试报告 — RIP 路由协议模拟系统

对照《网络编程课设任务书》**三、测试要求**（3.1–3.5）逐条说明。

## 3.1 测试环境

**要求**：3 台路由器全网状（mesh）拓扑。

**实现**：

```
     R1 ------- R2
      \         /
       \       /
        R3 ---
```

- R1: UDP 5201 / TCP 8021，直连 10.0.1.0/24
- R2: UDP 5202 / TCP 8022，直连 10.0.2.0/24
- R3: UDP 5203 / TCP 8023，直连 10.0.3.0/24

**启动**：

```bash
./ripd config/router1.conf 2>/tmp/r1.log &
./ripd config/router2.conf 2>/tmp/r2.log &
./ripd config/router3.conf 2>/tmp/r3.log &
```

---

## 3.2 基本功能测试 & 路由收敛验证

**要求**：三台路由器启动后，能正确交换路由信息，每台路由表中应有 3 条路由。

**实现位置**：`src/router.c` → `router_run()` 启动时发送初始路由表 + 30 秒周期更新；`src/rip.c` → `rip_recv()` 接收并调用 `rt_upsert()` 更新路由表。

**理想输出**（`nc 127.0.0.1 8021` → `show route`）：

```
Destination              PfxLen Next Hop                 Metric  From
--------------------------------------------------------------------------
10.0.1.0                 24     0.0.0.0                  1       direct
10.0.2.0                 24     127.0.0.1                2       neighbor
10.0.3.0                 24     127.0.0.1                2       neighbor

Total: 3 routes
```

**说明**：直连路由 metric=1，经由邻居学到的路由 metric=2（邻居通告 metric=1，Bellman-Ford +1）。三台路由器各自都有 3 条路由。

---

## 3.3 毒性反转测试

**要求**：手动 `link down <neighbor_id>` 后，该邻居学来的路由应被立即毒化（metric=16），标记 `[GC]`。

**实现位置**：
- `src/tcp_mgmt.c` → `cmd_link_down()` 调用 `nt_set_active(idx, 0)` + `rt_poison_from_neighbor(idx)`
- `src/route_table.c` → `rt_poison_from_neighbor()` 将该邻居的所有路由 metric 设为 16
- `src/rip.c` → `rip_send_response()` 毒性逆转：发给邻居时，从该邻居学到的路由 metric 设为 16

**测试步骤**：

```bash
nc 127.0.0.1 8021
> link down 2
OK: link DOWN to neighbor '2', 1 routes poisoned

> show route
Destination              PfxLen Next Hop                 Metric  From
--------------------------------------------------------------------------
10.0.1.0                 24     0.0.0.0                  1       direct
10.0.2.0                 24     127.0.0.1                16      neighbor [GC]  ← 已毒化
10.0.3.0                 24     127.0.0.1                2       neighbor

> link up 2
OK: link UP to neighbor '2', RIP request sent             ← 恢复
```

**说明**：毒性逆转防止路由环路——R1 向 R3 发送更新时，将来自 R3 的路由 metric 设为 16（避免 R3 再把 R1 当作到达该网络的出口）。手动 `link down` 用于快速验证。

---

## 3.4 邻居失效自动检测

**要求**：
1. 180 秒未收到邻居更新 → 自动判定失效，毒化相关路由
2. metric=16 的路由保留 120 秒后被垃圾回收删除

**实现位置**：
- `src/neighbor.c` → `nt_check_timeouts()` 遍历邻居，检查 `now - last_recv > 180`
- `src/router.c` → `check_neighbor_timeouts()` 调用 `nt_set_active(idx, 0)` + `rt_poison_from_neighbor(idx)`
- `src/route_table.c` → `rt_garbage_collect()` 删除 metric=16 且 `now - last_updated >= 120` 的条目

**测试步骤**：

```bash
# 1. 直接杀死 Router 2 进程
pkill -f "ripd.*router2"

# 2. 等待 180 秒后检查 Router 1
nc 127.0.0.1 8021
> show neighbors
ID       Address                State  Last Recv
--------------------------------------------------------
2        127.0.0.1:5202         DOWN   14:36:04          ← 自动检测为 DOWN
3        127.0.0.1:5203         UP     14:39:04

> show route
Destination              PfxLen Next Hop                 Metric  From
--------------------------------------------------------------------------
10.0.1.0                 24     0.0.0.0                  1       direct
10.0.2.0                 24     127.0.0.1                16      neighbor [GC]  ← 自动毒化
10.0.3.0                 24     127.0.0.1                2       neighbor

# 3. 再等 120 秒
> show route
Destination              PfxLen Next Hop                 Metric  From
--------------------------------------------------------------------------
10.0.1.0                 24     0.0.0.0                  1       direct
10.0.3.0                 24     127.0.0.1                2       neighbor        ← 10.0.2.0/24 已删除

Total: 2 routes
```

**说明**：
- 被动超时检测（方式一）：进程崩溃后 180 秒自动检测，无需人为介入
- 垃圾回收：毒化路由 120 秒后自动从路由表删除，释放表空间
- **修复记录**：`route_table.c` Bellman-Ford 规则 2 原先仅比较 `next_hop` 地址和 metric，多邻居共用 127.0.0.1 时会导致邻居 B 发来的同 metric 更新错误刷新邻居 A 的 `last_updated`，GC 计时器被反复重置。修复后加入 `e->from_neighbor == from_neighbor` 条件确保只有同一来源的更新才刷新时间戳。

---

## 3.5 TCP 管理接口测试

**要求**：通过 `telnet`/`nc` 连接管理端口，执行 `show route`、`show neighbors`、`link down <id>`、`link up <id>`、`quit`。

**实现位置**：`src/tcp_mgmt.c` → `mgmt_client_handler()` 命令解析 + 调用各类处理函数。

**测试**：

```bash
$ nc 127.0.0.1 8021
RIP Router Management Interface
Commands: show route | show neighbors | link down <id> | link up <id> | quit

> show route
Destination              PfxLen Next Hop                 Metric  From
--------------------------------------------------------------------------
10.0.1.0                 24     0.0.0.0                  1       direct
10.0.2.0                 24     127.0.0.1                2       neighbor
10.0.3.0                 24     127.0.0.1                2       neighbor
Total: 3 routes

> show neighbors
ID       Address                State  Last Recv
--------------------------------------------------------
2        127.0.0.1:5202         UP     14:36:04
3        127.0.0.1:5203         UP     14:36:04
Total: 2 neighbors

> link down 2
OK: link DOWN to neighbor '2', 1 routes poisoned

> link up 2
OK: link UP to neighbor '2', RIP request sent

> quit
Goodbye.
```

---

## 快速测试

运行完整测试脚本（需约 7 分钟，含 180s 超时等待）：

```bash
cd rip-sim
bash test_all.sh
```
