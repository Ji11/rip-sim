# 路由器 1 — 链状拓扑 R1—R2—R3，R1 只连 R2

id        = "1";
udp_port  = 5201;
tcp_port  = 8021;

neighbors = (
    { id = "2";  address = "127.0.0.1";  port = 5202; }
);

direct_networks = (
    { address = "10.0.1.0/24"; }
);
